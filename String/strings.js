// suppose a variable

let name = "abida nizam";

// strings are subscriptable or indexable

console.log(name[0]);

// string
console.log([name.length-4]);

//index 4
console.log(name[name.length[4]]);

// we can run loops on string 
for(let i =0; i<name.length; i++){
    console.log(name[i], 'string characters');
}